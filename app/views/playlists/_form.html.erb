<body class="bg-silver">
  <div class="row" style="margin-top:10em;">
    <%= form_for(@playlist, :html => {:class => 'col-sm-3 col-sm-offset-4 well'}) do |f| %>
      <% if @playlist.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@playlist.errors.count, "error") %> prohibited this playlist from being saved:</h2>

          <ul>
          <% @playlist.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>
      <h1 align="center">Lista</h1>
      <div class="row" style="margin-bottom: 2em;">
        <div class="col-sm-4 col-sm-offset-4"">
          <p class="text-center "style="width: 7em; height: 7em; border-radius: 100%; background-color: white; margin: 0 auto; padding-top:2em;">
            <span style="font-size: 3em; color: #597BB8;" class="fa fa-list"></span>
          </p>
        </div>
      </div>

      <div class="form-group">
        <%= f.label :name %><br>
        <%= f.text_field :name, class: 'form-control', :required => true %>
      </div>
      <div class="actions">
        <%= f.submit %>
      </div>
    <% end %>
  </div>
</body>


